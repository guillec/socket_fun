== Socket Fun
Just a fun learning experience task. The idea is to build multiple services with different languages. WHy?
Because I want to? Be cause I am a nerd?

First we have a Ruby TCP server that can handle a GET request. Also built a small Ruby browser to pull a web page.

